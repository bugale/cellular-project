#include "CommonHeader.h"

void main()
{
 	/*TMOD = 0x22;
	TH0 = 230;
	CMOD = 4;
	TR0 = 1;
	CR = 1;		//Timer pulse every 1 sec
	led0 = 0;
	led1 = 0;
	//relay0 = 0;  //SWITC RELAY TO RUN MODE
	while (CF==0); //Wait 1 sec for relay.
	CF = 0;
	led0 = 1;
	//printf("AT+CMGF=1 \n");  // Set modem to text mode
	while(1)
	{
	 	while(CF == 0);
		CF = 0;
		//printf("AT+CMGL=\"REC UNREAD\" \n");
		if (test == 0)	   //if--   then return to debug (wait to reset)
		{
			//relay0 = 1;
			while(CF==0);
			led1 = 1;
			while(1); 
		}                                                                                                                                                                     
	}*/
}